/**
 * Possible routes for the client-side app.  
 * **NOTE:** These should be renderable to the user, they are **NOT** API
 * endpoints!
 */
export const ROUTES = {
  'home': '/',
  'tech': '/tech/:tech_id',
  'error': '/error/:error_code'
};